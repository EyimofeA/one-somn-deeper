{
  "status": "conditional_not_run",
  "run_only_if": "local streaming validation is below 0.30",
  "anchor": "config.json",
  "one_change": "replace two local ConvGRU microsteps per source bit with one tied bidirectional GRU scan over all 11 positions",
  "mode": "exact_square",
  "seed": 74,
  "channels": 128,
  "scans_per_source_bit": 1,
  "source_bits": 22,
  "dropout": 0.09,
  "optimizer": "tuned_muon",
  "muon_learning_rate": 0.006,
  "muon_weight_decay": 0.1,
  "muon_warmup_steps": 250,
  "muon_lr_schedule": "constant",
  "batch_size": 512,
  "steps": 10000,
  "examples": 5120000,
  "compile": false,
  "selection": "unseen-x seen-N exact only",
  "audit_policy": "unseen-N is opened once after validation selection",
  "supervision": "final residue bits only",
  "submission_legal": false,
  "reason_not_submission_legal": "research diagnostic receives externally computed exact-square bits"
}
