# Recurrent VDF square→reduce diagnostic

Registered implementation for the clean two-digit-semiprime square→reduce
composition gate. The run is pending recovery of the active L40 SSH endpoint;
results will be appended only from its saved report or a clean rerun.

Source: `diagnostics/train_recurrent_vdf_square_reduce.py`.
