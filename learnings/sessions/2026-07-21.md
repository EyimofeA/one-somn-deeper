# Session 2026-07-21 — combined lessons

Parent (UT/depth line) + Claude Code (place-value / Hard). Full append-only trail: `solving/RESEARCH_LOG.md`. Living scoreboard: `solving/STATUS.md`.

## Where we ended

- Hard **#11 · 0.03%** (Claude `claude_hard_h1`). Leaderboard top **0.40%**. Near-tie from ~0.05% down.
- Hard train **100% exact / loss 0** with eval **0%** on test / ood_t / ood_n_t → **memorized, did not transfer**.
- Medium topped out ~**0.2%** after fixing the LR schedule. Still pre-grok on 600s (~58k steps; transition ~64k on Hard).

## What worked

| Lesson | Evidence |
|--------|----------|
| Small width + tied loops | d=32 beat wider under Easy clock |
| UT depth embeddings | vs plain loops; e5 best **1.00%** (`depth_d32_k4_ut`) |
| Train K=2 / eval K=4 | e1 **6.80%** stable ×3 |
| Place-value field+place emb | Claude +**1.16 pp** on e1 vs UT K4 anchor (bit-reproducible) |
| Fix cosine sawtooth | Medium steps ≫ `T_max=8×seconds` → LR restarted; clamped/optsched ~**2×** m5 (0.09→0.20%) |
| Prefer inv-sqrt / no fixed T_max | `15-lr-schedules-wallclock.md` |

## What failed / rejected

| Idea | Outcome |
|------|---------|
| N-cond FiLM | e5 worse |
| Soft ACT (no ponder) | e1 down |
| Midloop (T²MLR depth map) | e1 0.83%; train exact high, eval dead |
| More width on Medium/Hard | Groks train, eval 0 — not a capacity gap |
| Depth = T | Throughput + OOD collapse (iteration not exact) |
| Easy vanity / solvers | Discord: Easy 100% often hardwired; Hard ≠ Easy algorithm |

## Critical corrections

1. **e1 is a weak ranking signal.** Fixed N=323; OOD T≥4 has a tiny answer set; ood≈9% is near-trivial. Prefer **e5 + Medium**. Detail: `16-representation-vs-throughput.md`.
2. **Train batch exact ≠ score.** Peaks 30–34% paired with ~1% mean when overfitting.
3. **Noise:** same seed 74; e1 often bit-stable; e3 not. Protocol in `experiments/OPS.md`.
4. **Aux is not logged** — only useful inside custom loss.
5. **Iterate locally** in `competition/` before burning quota.

## Naming (keep)

- **d** = width · **K** = loop count · **T** = problem squarings  
- Docs: `12-current-arch-and-naming.md`, `13-decisions-glossary.md`

## Next research direction

Not more params. Force a **reusable single step** with error reset (re-quantise digits between loops). Full proposal: `17-recurrence-generalisation.md`. First cheap knob: **higher weight decay**.
